package com.atguigu.chapter05

object scala_match {
  def main(args: Array[String]): Unit = {

//      var a = 10
//      var b = 20
//      var operator = "+"
//    val result = operator match {
//      case "+" => a + b
//      case _ => "other"
//    }
//    println(result)
//
//    def abs(x: Int) = {
//      x match {
//        case i: Int if i >= 0 => i
//        case j: Int if j < 0 => -j
//        case _ => "type illegal"
//      }
//    }
//
//    new Array[String](3)
//    //new String[3]
//
//    println(abs(-5))
//
//    def describe(x: Any) = x match {
//      case m: List[Int] => "List"
//    }
//    println(describe(List(1, 2, 3, "s"))) //List
//
//  }
//
//
//  for (arr <- Array(
//    Array(0),
//    Array(1, 0),
//    Array(0, 1, 0),
//    Array(1, 1, 0),
//    Array(1, 1, 0, 1),
//    Array("hello", 90))) { // 对一个数组集合进行遍历
//
//    val result = arr match {
//      case Array(1,_) => "1" //匹配Array(0) 这个数组
//
//      case Array(x, y) => x + "," + y //匹配有两个元素的数组，然后将将元素值赋给对应的x,y
//
//      case Array(0, _*) => "以0开头的数组" //匹配以0开头和数组
//
//      case _ => "something else"
//    }
//
//    println("result = " + result)












  }
}
